const { makeNodes, ListNode } = require("./utils/LinkedList");

/**
 * @param {ListNode} head
 * @return {number[]}
 */
const nextLargerNodes = (head) => {
  const stack = [];

  

  while (head) {
    if()

    head = head.next;
  }

  console.log(stack);
};

nextLargerNodes(makeNodes([2, 1, 5]));
